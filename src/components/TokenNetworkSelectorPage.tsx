import { type FunctionComponent, type ReactNode } from 'react'

interface TokenNetworkSelectorPageProps {
  name: string
}

export const TokenNetworkSelectorPage: FunctionComponent<TokenNetworkSelectorPageProps> = (): ReactNode => {
  return (
    null
  )
}
